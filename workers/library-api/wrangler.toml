name = "library-api"
main = "src/index.ts"
compatibility_date = "2024-01-17"

# 自定义域名路由 - 让中国大陆用户可以直接访问
routes = [
  { pattern = "api.hiic.ai/*", zone_name = "hiic.ai" }
]

[[r2_buckets]]
binding = "HIIC_LIBRARY"
bucket_name = "hiic-library"
