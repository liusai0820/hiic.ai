# HIIC AI Lab Design Guide

本项目是深圳国家高技术产业创新中心的 AI 门户，视觉目标是“科技产业智库 + 可信研究能力 + 亲和的 AI 导览”。页面不应呈现为纯黑白研究站，也不应做成过度娱乐化的潮玩页面。

## 设计原则

- 专业可信：布局要克制、清晰、可扫描，优先服务研究、应用、Skill 和报告展示。
- 科技产业感：使用深国创品牌蓝和冷色科技辅助色，避免大面积黑白灰。
- 亲和但不低龄：小七是智慧伙伴与文化 IP，应按页面语境自然出现，不能像统一 AI 助手组件一样生硬插入。
- 内容可维护：所有新增栏目和详情页优先从 `content/portal.json` 读取。
- Agent 友好：页面、文档、结构化索引必须方便搜索引擎和 AI agent 理解。

## Logo

- 正式机构锁定组合资产为 `public/brand/hiic-official-lockup-white.png`。
- 当前 Header 只使用左侧 HIIC 标识：`public/brand/hiic-logo-mark-white.png`。如果用户提供新的公司 logo，优先替换这个文件，不改页面结构。
- `AI Lab` 作为门户属性文字跟随 logo 出现，不替代正式机构 logo。
- 不再使用临时星星图标或单独的 `AI` 方块作为主 logo。

## 品牌色

从《深国创中心品牌 IP 使用规范》提取并适配到网页：

| Token | Hex | 用途 |
| --- | --- | --- |
| `--hiic-brand-blue` | `#0A04AE` | 主按钮、重点标签、品牌色块 |
| `--hiic-brand-indigo` | `#3B36BE` | 辅助按钮、装饰线、渐层过渡 |
| `--hiic-tech-blue` | `#1F6FEB` | 链接、数据高亮、图表主色 |
| `--hiic-cyan` | `#31B7F0` | 科技感辅助线、动效光标、轻提示 |
| `--hiic-sky` | `#EAF3FF` | 浅色信息区背景 |
| `--hiic-ink` | `#111827` | 正文主文本 |
| `--hiic-muted` | `#52637A` | 次级文本 |

小七 IP 配色应保持规范原色，不在代码里重新上色：

| 元素 | 3D 色值 | 2D 色值 |
| --- | --- | --- |
| 头发 | `#1C251D` | `#2D2E28` |
| 面部 | `#F4D3C0` | `#FCE5CD` |
| 七哥背带裤 | `#5172AB` | `#476C9B` |
| 七妹背带裤 | `#567E9E` | `#5E87B2` |
| 皮鞋 | `#975C47` / `#B78E6F` | `#934C30` / `#D1AF82` |

## IP 使用规范

小七有两个形象：七哥、七妹。两者定位为深国创中心的智慧伙伴与文化 IP，性格是聪慧敏锐、真诚可靠、富有探索精神。

允许：

- 使用规范 PDF 中提取的原始 2D / 3D PNG 素材。
- 在页面中做轻量位移、漂浮、呼吸、淡入等网页动效。
- 在首页路径提示、资讯时间轴、应用/Skill/报告详情、空状态、报告入口中按场景使用不同小七素材。
- 七哥、七妹、双人组合、阅读/看板/成果姿态要按页面语境切换；默认不允许全站只用一张小七。
- 导览文案必须跟当前页面内容相关，不能复用“我是 AI 助手”式通用句。
- 根据场景搭配科技界面、报告卡片、蓝色信息面板等道具背景。

禁止：

- 修改小七比例、发型、呆毛方向、服装、五官比例、背带裤颜色。
- 给小七添加不符合智库定位的潮玩、恶搞、低龄化元素。
- 过度裁切头发、呆毛、脸部、背带裤标识等关键结构。
- 放在花哨、复杂、低识别度背景上。
- 用 CSS 手绘、emoji、占位图或非规范插画替代小七。
- 全站复用同一张小七图和同一句导览文案。
- 使用突兀的侧边色条、AI 助手式固定组件、过强边框来承载小七。
- 把小七做成每页相同的客服入口、聊天气泡或悬浮广告。

## 页面规范

### 首页

- 第一屏保留“AI Research & Capability Hub”的专业结构，但应加入品牌蓝色带、小七品牌伙伴和科技信息层。
- Header 必须让正式机构 logo 成为第一眼可见的品牌信号，`AI Lab` 只能作为门户属性说明。
- 首页不做营销式大 hero，不堆大面积渐变球和装饰图案。
- 首页的“打开应用”和“查看详情”必须区分清楚：卡片进入详情页，按钮打开应用。
- 小七在首页作为品牌伙伴自然出现，避免独立成“导览机器人”。

### 栏目页

- 应用、Skill、教程、报告、专题可以使用卡片式布局。
- 资讯/观察不使用卡片网格，优先使用时间轴或列表流，突出日期、主题和连续更新感。
- AI 咨询实时动态优先从 AI HOT REST API 接入，RSS 仅作为内部维护备选，不把实时内容写死进 `content/portal.json`。
- 资讯页实时 AI 资讯必须并入主时间轴，默认同步 50 条全量流，不做右侧资讯卡，不在用户界面展示“来源 / RSS / REST API”等技术维护入口。
- 每个栏目页都必须保留搜索、分类筛选和 Agent 索引入口。

### 详情页

- 标题区要突出内容身份、状态、标签和主要行动。
- 右侧信息卡展示封面、关键指标、作者、更新日期、Agent metadata。
- Skill 详情页主体必须呈现 `SKILL.md` 风格的完整文档，包含输入、输出、执行流程、验收标准、示例输入和可复制 Prompt。
- 小七可作为语境辅助：应用页偏工具巡视，Skill 页偏方法提示，教程页偏学习陪跑，资讯页偏观察记录，报告页偏成果讲解。
- 小七模块应该像页面角落里的插画旁注，不要每个详情页都使用相同素材或相同文案，不要遮挡正文或主要 CTA。

## 动效规范

- 小七导览员可使用 4-6 秒循环的轻微上下浮动。
- 时间轴节点可使用 hover 状态、细线推进、轻微蓝色高亮。
- 避免高频闪烁、夸张弹跳、GIF 式循环干扰阅读。
- `prefers-reduced-motion` 下应关闭主要动效。

## SEO / GEO

- 每条内容有独立 URL。
- 构建时必须运行 `npm run generate:seo`。
- 职场办公 Skill 必须按真实工作场景分类，例如职场办公、PPT 制作、数据处理、数据分析、咨询研究、研究报告、财务分析、行政协同、营销设计、人力资源。
- 对外索引文件包括：
  - `public/sitemap.xml`
  - `public/feed.json`
  - `public/llms.txt`
  - `public/ai-index.json`
  - `public/data/*.json`

## 验收清单

- 颜色是否明显使用深国创品牌蓝，而不是黑白灰。
- Header 是否使用正式 HIIC 机构 logo，而不是临时图标。
- 小七是否来自规范素材，且没有被改色、变形、过度裁切。
- 小七是否按页面语境自然出现，且素材/文案有差异。
- 资讯页是否是时间轴或列表流，不是普通卡片堆砌。
- 移动端是否没有文字重叠、按钮溢出、图片裁切关键结构。
- 新增内容是否同步更新 `content/portal.json` 和 SEO/GEO 文件。
